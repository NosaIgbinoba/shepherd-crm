## Copy to .env.local (gitignored) and fill in after creating a Supabase
## project — see SUPABASE_SETUP.md. Leave both unset to keep running on the
## built-in localStorage mock data.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
